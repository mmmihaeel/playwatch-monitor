{
  "name": "@playwatch/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --tsconfig tsconfig.typecheck.json src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@playwatch/config": "workspace:*",
    "@playwatch/db": "workspace:*",
    "@playwatch/storage": "workspace:*",
    "@playwatch/shared": "workspace:*",
    "fastify": "^5.8.2",
    "zod": "^4.1.11"
  }
}
